extends layout

block content
  a(href="/new") New Message 
  section 
    each message in messages
      div.message
        h3= message.text 
        p by   
          em= message.user
        p.date= `${message.added.toLocaleTimeString()}  ${message.added.toDateString()}`
